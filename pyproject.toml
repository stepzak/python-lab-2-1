[project]
name = "python-lab-2-1"
version = "0.2.0"
description = "Simple task source protocol"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.2",
    "jsonschema>=4.26.0"
]
[tool.pytest.ini_options]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]